server:
  port: 0
spring:
  application:
    name: loan
  data:
    mongodb:
      host: localhost
      port: 27020
      database: bookonloan
      username: loan
      password: loan
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    instance:
      preferIpAddress: true